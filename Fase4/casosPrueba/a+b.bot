$- a+b.bot:
 - ========
 - Recibe dos enteros 'a' y 'b', e imprime su suma.
 - Autor: Ricardo Monascal, 2016
 -$
create
  int bot a
    on activation:
      store 1.
      drop 2.
      collect as x.
    end
  end
  int bot b
    on activation:
      collect.
    end
    on 2 + 5 = 8:
      read.
      send.
    end
    on 1 + 3 = 4:
      store 1.
      drop 3.
    end
  end
execute
  activate a,b.
  advance a, b.
end

$$ puedes cambiar el 1 de la linea 22 por me y explota